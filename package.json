{
  "name": "ws-example",
  "version": "1.0.0",
  "description": "example for basic ws usage",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"light-server -s . -p 6090 --open",
    "socket":"node index.js",
    "all":"start npm run dev && start npm run socket"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "light-server": "^2.5.0",
    "ws": "^5.1.0"
  }
}
